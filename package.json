{
  "name": "sarboshakti-react-app",
  "version": "1.0.0",
  "description": "Sarboshakti Sanatani Sangathan - Full Stack React Application",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start",
    "build": "cd frontend && npm run build",
    "build:backend": "cd backend && npm install",
    "start:backend": "cd backend && npm start",
    "init-db": "cd backend && npm run init-db",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "deploy:frontend": "cd frontend && npm run build",
    "deploy:backend": "cd backend && npm install && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}